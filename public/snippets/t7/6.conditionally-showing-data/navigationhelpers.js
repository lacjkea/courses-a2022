//Our good old friend, grab the first nested element
//that match the selector
elem.querySelector(selector);

//Our new friend, grab all nested elements
//that match the selector
elem.querySelectorAll(selector);

//With one element selected, grab it's parent
elem.parentElement;

//With one element selected, grab it's next sibling
elem.nextElementSibling;

//With one element selected, grab it's previous sibling
elem.previousElementSibling;

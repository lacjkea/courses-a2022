const age = 43;
const email = "jofh@kea.dk";

let isEven;
if (age % 2 === 0) {
  even = true;
} else {
  even = false;
}

let role = "visitor";
if (email === "jofh@kea.dk") {
  if (age > 40) {
    role = "admin";
  }
}
//can be done in 1 line, if you merge the two if's
